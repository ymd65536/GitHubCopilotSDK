---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-agent
  namespace: a2a-copilot
  labels:
    app: a2a-agent
    agent: weather
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a2a-agent
      agent: weather
  template:
    metadata:
      labels:
        app: a2a-agent
        agent: weather
    spec:
      containers:
      - name: weather-agent
        image: a2a-agent:latest
        imagePullPolicy: Never  # Rancher Desktopでローカルイメージを使用
        command: ["python3", "weather_agent.py"]
        ports:
        - containerPort: 8001
          name: http
        env:
        - name: PORT
          value: "8001"
        - name: COPILOT_MODEL
          value: "gpt-5-mini"
        - name: COPILOT_GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: github-token
              key: token
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8001
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: weather-agent-svc
  namespace: a2a-copilot
spec:
  type: ClusterIP
  selector:
    app: a2a-agent
    agent: weather
  ports:
  - name: http
    port: 8001
    targetPort: 8001
